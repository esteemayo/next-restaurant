import Image from 'next/image';

const Menu = () => {
  return (
    <aside>
      <Image src='/img/open.png' width={20} height={20} alt='open' />
    </aside>
  );
};

export default Menu;
